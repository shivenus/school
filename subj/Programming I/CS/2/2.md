Deitel, il Linguaggio C
chapter 2 exercises

**2.3 (pag 65)**
a)
int number = 0;

b)
printf("%s", "input integer: ");

c)
scanf("%d", &a);

d)
if (number != 7) {
    printf ("number is not equal to 7");
}

e)
puts("This is a C program. ");

f)
puts("This is a C\nprogram. ");

g)
puts("This\nis\na\nC\nprogram. ");

h)
puts("This\tis\ta\tC\tprogram. ");


**2.4**
a
// computes the product of three ints

b
printf("%s", "enter three integers: ");

c
int x = 0;

d
int y = 0;

e
int z = 0;

f
scanf("%d%d%d", &x, &y, &z);

g
int result = x * y * z;

h
printf("The product is %d\n", result);


**2.5**
program based on 2.4


**2.7**
a
scanf("%d", &value);

b
printf("the product of %d and %d is %d\n", x, y, x * y);

c
sumOfNumbers = firstNumber + secondNumber;

d
if (number >= largest) {
	largest = number;
}

e
/* to compute three integers */

f
scanf("%d", &anInteger);

g
printf("Remainder of %d divided by %d is %d\n", x, y, x % y);

h
if (x == y) {
	printf("%d is equal to %d\n", x, y);
}

i
printf("the sum is %d\n", x + y);

j
printf("the value you entered is: %d\n", value);


**2.8**
a
I commenti

b
printf

c
if

d
di assegnazione

e
scanf


**2.9**
a
puts("Enter two numbers.");

b
a = b * c;

c
// this computes a salary

d
scanf("%d%d%d", &a, &b, &c);


**2.12**
a
2

b
4

c
x=

d
x=2

e
5 = 5

f
nothing

g
nothing

h
nothing

i
a newline character


**2.15**
a
x = 7 + 3 * 6 / 2 - 1;
	7 + 18 / 2 - 1;
	7 + 9 - 1;
	16 - 1
	15

b
x = 2 % 2 + 2 * 2 - 2 / 2;
	0 + 2 * 2 - 2 / 2;
	2 * 2 - 2 / 2;
	4 - 1;
	3

c
x = ( 3 * 9 * ( 3 + ( 9 * 3 / ( 3 ))));
	( 3 * 9 * ( 3 + ( 9 * 3 / 3 )));
	( 3 * 9 * ( 3 + ( 27 / 3 )));
	( 3 * 9 * ( 3 + 9 ));
	( 3 * 9 * 12 );
	27 * 12
	324
	

		 1
		 27
		x12
		---
		 54
		27
		---
		324


**2.**









