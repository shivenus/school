a) c = toupper(c);

b) printf("%s%s%s\n", "c is ", isdigit(c) ? "a " : "not a ", "digit");
or
printf("'%c'%sdigit\n", c, isdigit(c) ? " is a " : " is not a ");
or
printf("%c %s %s\n", c, isdigit(c) ? "is a" : "is not a", "digit");

c) printf("%s%s%s\n", "c is ", iscntrl(c) ? "a " : "not a ", "control character");
or
printf("%c %s %s\n", c, iscntrl(c) ? "is a" : "is not a", "digit");

d) fgets(s1, 100, stdin);

e) puts(s1);

f) ptr = strrchr(s1, c);

g) putchar(c);

h) printf("%c %s %s\n", c, isalpha(c) ? "is a" : "is not a", "letter");

i) c = getchar();

j) ptr = strstr(s1, s2);

k) printf("%c %s %s\n", c, isprint(c) ? "is a" : "is not a", "printing character");

l) sscanf("1.27 10.3 9.432", "%f%f%f", &d, &e, &f);

m) strcpy(s1, s2);

n) ptr = strpbrk(s1, s2);

o) printf("result is %d\n", strcmp(S1,S2);

p) ptr = strchr(s1, c);

q) sprintf(s1, "%7d%7d%7d", x, y, z);

r) strcat(s1, s2, 10);

s) int a =  strlen(S1);
printf("the length is %d\n", a);
printf("%s %d\n", "the length is", a);
or
printf("%s %d", "the length is", strlen(S1);
or
printf("strlen(s1) = %zu\n", strlen(s1));
//use %zu because it returns a size_t

t) ptr = strtok(s2, ",");



