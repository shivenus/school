a
int table[SIZE] [SIZE];

b
9
printf("%d\n", SIZE * SIZE);

c
for(size_t x = 0; x < SIZE; ++x) {
	for(size_t y = 0; y < SIZE; ++y) {
		table[x][y] = x + y;
	}
}

d
for(size_t x = 0; x < SIZE; ++x) {
	for(size_t y = 0; y < SIZE; ++y)
		printf("table[%d][%d] = %d\n", x, y, table[x][y]);
}
