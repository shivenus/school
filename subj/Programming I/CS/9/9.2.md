a
use %c, because argument is a character 'c'
or change argument to a string, "c" to print it as a string

b
use %% to print %

c
use %.1s

d
use \" to print quotes

e
missing double quotes inside printf for format control string (first argument), this will give an error

f
use %s to print x as a string "x"
or change argument to 'x' to print it as a character.

g
use double quotes for argument, since it's a string
